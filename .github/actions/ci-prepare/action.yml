name: Prepare CI environment
description: Generate varlock types and compile Paraglide messages.

runs:
  using: composite
  steps:
    - name: Generate varlock env types
      shell: bash
      run: APP_ENV=test pnpm exec varlock typegen

    - name: Compile paraglide
      shell: bash
      run: pnpm paraglide:compile
